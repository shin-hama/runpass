# CLAUDE.md — ランパス MVP

## プロジェクト概要

**ランパス**は、ランニング中のすれ違い体験を記録するスマートフォンアプリ。
ユーザーが走っている間、バックグラウンドでGPSを記録し、近くにいた他のランナーを
走行終了後にまとめて通知する。Nintendo DSの「すれ違い通信」をスマホのランニングで再現する。

**ターゲット：** ゆるく健康・交流目的で走る都市型ランナー（20〜40代）  
**コアバリュー：** 走るだけで、知らなかった「近所のランナー」と出会える  
**MVP目標：** すれ違い検出の核心ループを最短で動かすこと。UIの美しさより動作確認を優先する。

---

## 技術スタック

| レイヤー | 採用技術 | 理由 |
|---|---|---|
| モバイルアプリ | Expo (React Native) + TypeScript | バックグラウンドGPS取得が必要。`expo-location` で iOS/Android 両対応 |
| 認証 | Firebase Auth（匿名認証） | 初回摩擦を最小化。後からメール/Google連携に移行可能 |
| DB | Firestore | リアルタイム更新不要。読み書きシンプル |
| バックエンドロジック | Firebase Cloud Functions | すれ違い判定はサーバーサイドで実行（不正防止・負荷分散） |
| 通知 | Expo Push Notifications | Expo管理のトークンで iOS/Android を統一的に扱える |

---

## MVP スコープ

### 含める

- 匿名ユーザー登録（ニックネームのみ）
- ランニング開始・終了画面（距離・ペースのリアルタイム表示）
- バックグラウンドGPS記録 → Firestore保存
- 走行終了トリガーでCloud Functionがすれ違い判定を実行
- 「今日のすれ違い」一覧画面（匿名表示）
- ランナーカード詳細（すれ違い回数・ペース帯・エリア）
- Push通知（走行終了時）

### 含めない（後回し）

- ユーザー間のメッセージ・ラン仲間申請
- マップ画面（ランナー密度可視化）
- プレミアム機能・課金・SNSログイン・写真

---

## 設計上の非自明な判断

### すれ違い判定のトリガー設計
`runs/{runId}` の `status` が `'completed'` に更新されたときにCloud Functionsが発火する。
走行中はリアルタイム判定せず、終了後にまとめて処理する設計（バッテリー節約・コスト最小化）。

### encounters / locations への書き込みはCloud Functionsのみ
クライアントから直接書き込めない設計（Firestoreルール: `allow write: if false`）。
すれ違い判定の整合性を保つため、Admin SDKを持つCloud Functionsだけが書き込む。

### バックグラウンドGPS中のUID参照
バックグラウンドタスクはFirebase Auth Contextの外で動くため、UIDをAsyncStorageに保存して参照する。
`TaskManager.defineTask` はモジュールのトップレベルに書くこと（コンポーネント内に書くと動作しない）。

### オフライン時のlocation消失リスク
ネットワーク切断中はFirestoreクライアントがキューイングしてくれるが、プロセスが強制終了されると失われる。
MVPでもAsyncStorageへのローカルバッファ＋再送を入れておくことを推奨。

### すれ違い判定の計算量
15秒スロットでバケツ絞り込みを実施済み（時刻が近い点同士だけ距離計算する）。
ユーザーが増えたらGeoHashベースのクエリへ移行する必要がある。

---

## 開発環境の注意

**Expo Go ではバックグラウンドGPSが動作しない。必ず Development Build を使うこと。**

```
npx expo run:ios   または   eas build --profile development
```

環境変数は `.env.local` に設定する（`EXPO_PUBLIC_` プレフィックスでクライアントに公開）。`.gitignore` に含まれていることを確認すること。
